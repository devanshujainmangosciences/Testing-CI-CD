{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "baseUrl": "./src/", // all paths are relative to the baseUrl
    "paths": {
      // resolve any `@lib` to `<baseUrl>/lib`
      "components": ["components"],
      "actions": ["actions"],
      "apis": ["apis"],
      "assets": ["assets"],
      "constants": ["constants"],
      "navigation": ["navigation"],
      "reducers": ["reducers"],
      "screens": ["screens"],
      "utils": ["utils"],
      "store": ["store"]
    }
  }
}
