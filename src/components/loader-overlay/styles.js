/**
 * Stylesheet definition for LoaderOverloay component
 */
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  modalContainer: {
    margin: 0,
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
